# .NET Core SOAP Test

A simple C# console application to make a call to a SOAP web service and serialize the response as JSON.

## Uses:

* [SoapHttpClient](https://github.com/pmorelli92/SoapHttpClient)
* [Json.NET](http://www.newtonsoft.com/json)

Install Nuget packages in VS Code by doing:

```bash
~$ dotnet add package <package-name>
```